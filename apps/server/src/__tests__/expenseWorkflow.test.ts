import { expenseService } from '../modules/expense/expense.service';

describe('Expense Workflow State Machine', () => {
  it('should transition expense from DRAFT to SUBMITTED to APPROVED', async () => {
    expect(true).toBe(true);
  });
});